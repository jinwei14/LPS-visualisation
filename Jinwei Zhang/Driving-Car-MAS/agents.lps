processId(Agent), wantToMove(Agent, Pos2, T1, T2) ->
  Pos2 = position(X,Y)
  position(Agent, Pos, T3),
  nextMove(Agent, Pos2,T3,T4).


% processId(Self), borrow(Borrower, Self, Amount, T1, T2) ->
%   lend(Self, Borrower, Amount, T3, T4).

 <- nextMove(Agent, position(X,Y)),
    position(Agent2, Pos2),
    abs(pos)



nextMove() <-
  freeze().

<- false	eliminate, fire, not water.
