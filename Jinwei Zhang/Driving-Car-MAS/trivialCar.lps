maxTime(20).

loadModule(p2p).

p2pJoin(sharing, 4100).

fluent(position(X,Y)).
initially(position(car2,10,20)).
action(giving(X,Y)).

% observe(p2pReceive(lending, node(1, 2), borrow(10)), 2, 3).

p2pReceive(sharing, P, retrieving, T1, T2) ->
  Agent = car2
  position(Agent,X,Y),
  giving(X,Y, T2, T3),
  p2pSend(sharing, P, giving(X,Y)) from T2,
  lpsHalt from T3.

% updates(giving(Amt), money(A), money(A - Amt)).
